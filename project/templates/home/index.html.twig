{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

     <div class="row">
            {% if is_granted('IS_ANONYMOUS') %} 
                <div class="col-12 col-md-6 col-lg-3 mb-4">
                    <div class="card border shadow lift">
                        <div class="card-body">
                            
                           
                           {{ render_esi("app_login") }}
                         
                        </div>
                    </div>
                </div>
            {% endif %}

             {% if is_granted('ROLE_USER') %} 
                    <h1 class="text-center">Bienvenue sur tasker une application qui va vous permettre de gérer vos projets</h1>
              {% endif %}
        </div>

{% endblock %}
